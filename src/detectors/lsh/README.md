# LSH Detector

Locality-sensitive hashing for efficient code clone detection.

## Components

- `extractor.rs` - LSH signature extraction
- `lsh_cache.rs` - Caching layer for tokenization and signatures
- `config.rs` - LSH configuration
- `memory_pool.rs` - Memory-efficient pooling

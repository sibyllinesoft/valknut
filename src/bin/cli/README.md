# CLI Module

Command-line interface implementation.

## Components

- `commands.rs` - Command handlers (analyze, doc-audit, etc.)
- `args.rs` - Argument parsing
- `config_layer.rs` - Configuration loading
- `output/` - Output formatters (JSON, CSV, Sonar, etc.)

<!-- Hero Section with Animated Background -->
{{#if enable_animation}}
<div class="hero-container">
    <canvas id="neural-network" class="neural-background"></canvas>
    <div class="hero-content">
        <div class="hero-logo-container">
            <img src="{{logo_data_url}}" alt="Valknut Logo" class="hero-logo">
        </div>
        <div class="hero-title-container">
            <h1 class="hero-title">{{tool_name}} Analysis Report</h1>
        </div>
        <p class="hero-subtitle">Comprehensive code quality analysis and refactoring guidance</p>
    </div>
</div>
<hr class="hero-divider">
{{else}}
<div class="hero-container no-animation">
    <div class="hero-content">
        <div class="hero-logo-container">
            <img src="{{logo_data_url}}" alt="Valknut Logo" class="hero-logo">
        </div>
        <div class="hero-title-container">
            <h1 class="hero-title">{{tool_name}} Analysis Report</h1>
        </div>
        <p class="hero-subtitle">Comprehensive code quality analysis and refactoring guidance</p>
    </div>
</div>
<hr class="hero-divider">
{{/if}}

<div class="container">
    <!-- Report Metadata -->
    <div class="meta" style="display:flex; align-items:center; justify-content: space-around; gap: 12px; margin-bottom: var(--space-xl); color: var(--color-text-muted); font-size: var(--text-sm);">
        <span>Generated: {{generated_at}} • Version: {{version}}</span>
        <button id="debug-open" type="button" aria-label="Show raw analysis JSON"
            style="display:inline-flex; align-items:center; gap:4px; background: transparent; border: none; color: var(--color-text); padding: 0; cursor: pointer; font-size: 0.9rem;">
            <span data-lucide="bug" aria-hidden="true" style="width:14px;height:14px;"></span>
            <span>JSON</span>
        </button>
    </div>
